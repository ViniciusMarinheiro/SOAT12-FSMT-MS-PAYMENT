apiVersion: v1
kind: Service
metadata:
  name: payment-service
spec:
  type: LoadBalancer # 👈 Mudou de ClusterIP para LoadBalancer
  selector:
    app: payment-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001